# Build output is redirected outside the repo entirely - a target/ folder
# living inside frontend-dist's parent tree would get bundled into itself,
# growing without bound. Separate target-dir from the other two desktop
# apps' (20below, combat-tracker) so the builds don't clobber each other.
[build]
target-dir = "C:/Users/feral/.tauri-targets/brewer"
